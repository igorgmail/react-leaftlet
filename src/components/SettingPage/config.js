const configAdminPage = {
  // Если у полученного с сервера объекта есть address тогда мы не получаем его по API
  updateAddress: true,
  // При обновлении поля(input) запускается спиннер(BackDrop)
  showBackDrop: true,
}

export default configAdminPage;